# InfluxDB Configuration for Local Development
# Copy this file to .env.local and update the values
# IMPORTANT: .env.local is gitignored and should NEVER be committed

# InfluxDB Admin Credentials
INFLUX_ADMIN_USER=admin
INFLUX_ADMIN_PASSWORD=admin12345

# InfluxDB Organization & Bucket
INFLUX_ORG=energy-monitor
INFLUX_BUCKET=predictions

# InfluxDB Admin Token (generate a secure random string, min 32 characters)
# You can generate one with: openssl rand -hex 32
INFLUX_TOKEN=your-super-secret-admin-token-here-min-32-characters

# Local API Server Configuration
LOCAL_API_PORT=3001

# InfluxDB URL (default: localhost for local development)
INFLUX_URL=http://localhost:8086

# CORS Origin (default: localhost frontend)
CORS_ORIGIN=http://localhost:8080

# Inference Service Configuration
INFERENCE_SERVICE_URL=http://localhost:8000
USE_ML_INFERENCE=true

# Node environment (development/production)
NODE_ENV=development
