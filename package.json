{
  "name": "nilm-energy-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "NILM Energy Monitor - Energy disaggregation with deep learning",
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "preview": "npm run preview --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=apps/web",
    "format": "npm run format --workspace=apps/web",
    "format:check": "npm run format:check --workspace=apps/web",
    "test": "npm run test --workspace=apps/web",
    "test:watch": "npm run test:watch --workspace=apps/web",
    "predictions:seed": "tsx scripts/write-to-influx.ts",
    "predictions:verify": "tsx scripts/verify-influx.ts"
  },
  "workspaces": [
    "apps/web"
  ],
  "devDependencies": {
    "@influxdata/influxdb-client": "^1.35.0",
    "@types/papaparse": "^5.5.2",
    "dotenv": "^17.2.3",
    "papaparse": "^5.5.3",
    "tsx": "^4.21.0"
  }
}