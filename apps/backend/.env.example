# Server Configuration
ENV=dev
DEBUG=true
PORT=8000
HOST=0.0.0.0

# CORS (Comma separated list of allowed origins)
# For prod: https://<your-project>.pages.dev,https://<your-custom-domain>
CORS_ORIGINS=http://localhost:5173,http://localhost:8080

# InfluxDB Configuration (External/Hosted for Prod)
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=my-admin-token
INFLUX_ORG=docs
INFLUX_BUCKET_RAW=raw_sensor_data
INFLUX_BUCKET_PRED=predictions

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
# Provide the anon key (public)
SUPABASE_ANON_KEY=eyJ...
# Provide the JWT secret (HS256) if using legacy signing, OR ensure JWKS works
SUPABASE_JWT_SECRET=super-secret-jwt-token-hmac-sha256

# Auth Configuration
# Verify 'aud' claim in JWT matches authenticated project
AUTH_VERIFY_AUD=true

# Redis Configuration (Optional - falls back to in-memory cache)
# For local development: REDIS_URL=redis://localhost:6379
# For Railway private network: REDIS_URL=redis://default:PASSWORD@redis.railway.internal:6379
# REDIS_URL=

# Admin Security
# Token required for /admin endpoints
ADMIN_TOKEN=change-me-to-a-strong-secret
