# ============================================
# SUPABASE CONFIGURATION (Required)
# ============================================
# Get these values from: Supabase Dashboard > Settings > API
VITE_SUPABASE_URL="https://your-project.supabase.co"
# Publishable Key (safe to expose in browser)
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"
# (VITE_SUPABASE_ANON_KEY is also supported as an alias)

# Optional: Use Project ID instead of full URL (convenient for some setups)
# VITE_SUPABASE_PROJECT_ID="your-project-id"

# Backend API URL
# For local dev, defaults to "/api" (Vite proxy) if not set.
# For production/deployment, set the full URL:
VITE_BACKEND_URL="https://energy-monitor.up.railway.app"
# For explicit local override (not usually needed):
# VITE_BACKEND_URL="http://localhost:8000"

# ============================================
# DEMO MODE (Optional)
# ============================================
# Set to 'true' to enable demo login UI and auto-login functionality
VITE_DEMO_MODE="false"
VITE_DEMO_EMAIL="demo@example.com"
VITE_DEMO_PASSWORD="demo123"
VITE_DEMO_USERNAME="demo"

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
# 1. NEVER commit your actual .env file to version control
# 2. The VITE_SUPABASE_ANON_KEY is the "anon" key - it's safe to expose
#    in the browser ONLY because RLS (Row Level Security) protects your data
# 3. NEVER use the service_role key in the frontend - only in Edge Functions
# 4. For production, set these as Cloudflare Pages environment variables
# ============================================

# For local development, copy this file to .env and fill in your values:
# cp .env.example .env

# ============================================
# LOCAL INFLUXDB MODE
# ============================================
# Set to 'true' to use local InfluxDB instead of cloud Supabase
# When enabled, the dashboard will fetch predictions from a local InfluxDB instance
# This requires:
# 1. Docker and Docker Compose installed
# 2. InfluxDB running: docker compose up -d
# 3. Predictions seeded: npm run predictions:seed
# 4. Local API server running: npm run local:server (or npm run local:dev)
VITE_LOCAL_MODE="false"

# ============================================
# DEPRECATED ENV VARS (for backward compatibility)
# ============================================
# These are supported but legacy:
# - VITE_SUPABASE_ANON_KEY → alias for VITE_SUPABASE_PUBLISHABLE_KEY
# - VITE_API_BASE_URL → use VITE_BACKEND_URL
