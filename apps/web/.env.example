# ============================================
# SUPABASE CONFIGURATION (Required)
# ============================================
# Get these values from: Supabase Dashboard > Settings > API
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"

# ============================================
# BACKEND API CONFIGURATION
# ============================================
# For LOCAL development: Leave empty to use Vite proxy (/api -> http://localhost:8000)
# For PRODUCTION: Set to your deployed backend URL
# Example: VITE_API_BASE_URL="https://your-backend.railway.app"
VITE_API_BASE_URL=""

# ============================================
# DEMO MODE (Optional)
# ============================================
# Set to 'true' to enable demo login UI and auto-login functionality
VITE_DEMO_MODE="false"
VITE_DEMO_EMAIL="demo@example.com"
VITE_DEMO_PASSWORD="demo123"
VITE_DEMO_USERNAME="demo"

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
# 1. NEVER commit your actual .env file to version control
# 2. The VITE_SUPABASE_PUBLISHABLE_KEY is the "anon" key - it's safe to expose
#    in the browser ONLY because RLS (Row Level Security) protects your data
# 3. NEVER use the service_role key in the frontend - only in backend services
# 4. For production, set these as environment variables in your deployment platform
# ============================================

# For local development, copy this file to .env and fill in your values:
# cp .env.example .env
