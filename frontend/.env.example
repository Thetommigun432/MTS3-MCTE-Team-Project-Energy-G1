# Supabase Configuration
# Get these values from: Supabase Dashboard > Settings > API
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# Demo mode - set to 'true' to enable demo login UI and demo user creation
VITE_DEMO_MODE="false"

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
# 1. NEVER commit your actual .env file to version control
# 2. The VITE_SUPABASE_PUBLISHABLE_KEY is the "anon" key - it's safe to expose
#    in the browser ONLY because RLS (Row Level Security) protects your data
# 3. NEVER use the service_role key in the frontend - only in Edge Functions
# 4. For production, set these as GitHub Secrets or Azure DevOps variables
# ============================================

# For local development, copy this file to .env and fill in your values:
# cp .env.example .env

# Demo mode - set to 'true' to enable demo login UI and demo user creation
# When enabled:
# - Shows demo credentials on the login page
# - Allows username shortcut (admin -> admin@demo.local)
# - Enables the "Log in as demo admin" button
VITE_DEMO_MODE="false"

# Demo credentials (only used when VITE_DEMO_MODE="true")
# Set these in your .env file to configure demo login credentials
VITE_DEMO_EMAIL="demo@example.com"
VITE_DEMO_PASSWORD=""
VITE_DEMO_USERNAME="demo"
