# =============================================================================
# NILM Energy Monitor - Root Environment Variables
# Copy to .env and fill in values
# =============================================================================

# =============================================================================
# InfluxDB (required for backend)
# =============================================================================
INFLUX_TOKEN=admin-token              # Default for local dev (matches compose.yaml)
INFLUX_ORG=energy-monitor
INFLUX_BUCKET_PRED=predictions        # Bucket for predictions (model output)
INFLUX_BUCKET_RAW=raw_readings        # Bucket for raw sensor readings

# Optional InfluxDB admin config (for local Docker init)
INFLUX_ADMIN_USER=admin
INFLUX_ADMIN_PASSWORD=admin12345      # Change in production!

# =============================================================================
# Supabase (required for auth)
# =============================================================================
SUPABASE_URL=                         # From Supabase dashboard
SUPABASE_PUBLISHABLE_KEY=             # From Supabase dashboard (formerly ANON_KEY)
# SUPABASE_JWT_SECRET=                # LEGACY FALLBACK (HS256 only). Prefer JWKS.
# SUPABASE_ANON_KEY=                  # Legacy fallback for PUBLISHABLE_KEY

# =============================================================================
# Data Source Configuration (for simulator)
# =============================================================================
# RAW_DATA_SOURCE controls where the simulator reads raw data from:
# - "local"  = Read from parquet file (default, for local development)
# - "influx" = Read from INFLUX_BUCKET_RAW (for Railway/cloud deployment)
RAW_DATA_SOURCE=local

# =============================================================================
# Dataset & Model Paths
# =============================================================================
DATASET_PATH=/app/data/simulation_data.parquet
MODELS_DIR=/app/models
